# Jubakit Documentation

To build the document:

```
pip install Sphinx sphinx_rtd_theme
./build.sh
```

Note that all jubakit dependency modules (currently `tweepy` and `jq`) must be installed to build a complete document.
